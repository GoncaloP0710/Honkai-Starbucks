"use strict";
var __importDefault = (this && this.__importDefault) || function (mod) {
    return (mod && mod.__esModule) ? mod : { "default": mod };
};
Object.defineProperty(exports, "__esModule", { value: true });
exports.fetchJSON = fetchJSON;
const axios_1 = __importDefault(require("axios"));
const json_bigint_1 = __importDefault(require("json-bigint"));
const JSONBig = (0, json_bigint_1.default)({ useNativeBigInt: true });
// do not transform response
const transformResponse = [(data) => data];
async function fetchJSON(url, client, enableTimeout = false, parseBigint = false) {
    const headers = { "User-Agent": client.options.userAgent };
    if (client.options.githubToken && url.startsWith("https://api.github.com/"))
        headers["Authorization"] = `Bearer ${client.options.githubToken}`;
    const options = { headers, transformResponse };
    if (enableTimeout)
        options.timeout = client.options.requestTimeout;
    const res = await (async () => {
        try {
            return await axios_1.default.get(url, options);
        }
        catch (e) {
            if (typeof e === "object" && e && "response" in e)
                return e.response;
            else
                throw e;
        }
    })();
    if (res.data) {
        try {
            res.data = parseBigint ? JSONBig.parse(res.data) : JSON.parse(res.data);
        }
        catch {
            // do not parse if it is not json due to some error
        }
    }
    return res;
}
